version: "3.8"

services:
  tweetagent:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./db.sqlite3:/app/db.sqlite3
      - ./charts:/app/charts
    restart: unless-stopped
